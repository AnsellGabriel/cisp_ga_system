<%= turbo_stream.update "remote_modal_body" do %>
  <%= render "form", url: votes_path(e: @event_hub, c: @candidate), meth: 'post' %>
  <%# <%= render "form", url: benefit_claims_path(v: @process_claim), meth: 'post' %> 
<% end %>