<%# <div class="card">
  <div class="card-header">
  <h5 >Edit vote</h5>
  
  </div>

  <div class="card-body"> %>
  <%= render "remote_modal", title: "Edit vote" do %>
    <%#= render "form", vote: @vote %>
    <%= render "form", url: vote_path(@vote, p: params[:p]), meth: 'patch' %>
    <%end%>
  <%# </div>
</div> %>